# Optional. Get a free project id at https://cloud.reown.com if you want
# WalletConnect (mobile wallet) support in addition to browser-injected wallets
# (MetaMask, Rabby, OKX, etc, which work with no env vars at all).
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=
